# ワンタップタイマー 実装仕様

決めたこと（2026-09-09）。プロトタイプ `prototype/index.html` の B 案「ドレイン」。

## 要件

- **止めるのはクラウンだけ。** 長押しでも止められるようにしていたが、長押しではアプリを閉じられない
  （watchOS に終了の API が無い）。クラウンなら「止めて文字盤へ」が一動作で済むので、そちらへ寄せた
- 止めた状態は**白い終了画面にしない**。出ていく瞬間に画面が白く光る。満タン＋設定の長さで「待っている」見た目にする
- **裏では動かさない。** クラウンを押して出た時点でタイマーも予約した通知も片付ける
- **走っている間は文字盤に戻さない**（`WKExtendedRuntimeSession` / `self-care`）。
  これが無いと1分ほどで文字盤へ戻り、合図が通知頼りになって十数秒ずれる
- **終わったら、クラウンを押したのと同じところまで片付ける**（前面に留めるのをやめる・通知を消す・次に開いたら新しく始める）。
  留めるのをやめると watchOS がいつもどおり文字盤へ戻していく
- 終了後に**アプリを自分で閉じる仕組みは watchOS に無い**（公開 API が無い）。
  「すぐ閉じる」はここまでが限界。即座に閉じたいときはクラウンを押す

- **ワンタップ**：開いたら設定済みの長さで即開始。設定画面を経ない
- 終了は**一度だけ**震える。鳴り続けない。自分で触らなくてよい
- 走っている最中に**設定画面**へ入れる（左上の歯車。上部に、目立たない質感で、押しやすい大きさ）
- 長さは **10秒〜60分**。初期値 90秒
- **止めるボタンは無い**。画面タップは最初から
- **1分を切ったら秒だけ**を出す
- App Store に出す（iOS の器つき）

## 画面

### 走っている
- 地は黒。下から溜まった水（ティール）が、残りの割合ぶんの高さ。水面に白い線
- 真ん中に残り。`m:ss`（`1:30`）。1分を切ったら秒だけ（`45`）を一回り大きく
- **走っている間は数字だけ。** 「のこり」も「秒」も出さない。添えないぶん、数字が画面のちょうど真ん中に来る
- 最後の10秒は水が琥珀（長さが20秒以下なら出さない）
- 上の中央に丸ボタン（42pt。ストップウォッチの絵だけ。印を重ねると小さい丸の中がうるさい）
- **右端の上から4分の1に「キャンセル ▶」**。その外側にクラウンがある。文字だけでは、どこを押すのか分からない
  （クラウンを左に設定していても画面ごと180度回るので、右端で合う）
- 走っている最中のタップは何もしない。長押し（0.7秒）でキャンセル。下端に「長押しでキャンセル」
- 終わった／キャンセルした画面はタップで新しく始める（下端に「タップで始める」）

### 設定（2026-09-10 実機で触って3度作り直し）
- 上から **−60s ／ +60s** → `1分30秒` → **−10s ／ +10s** → **色の丸 ／ 完了**
- 粗いほうを上に置く。「大きく動かしてから下で詰める」が上から下への動きになる
- 単位は `s` に統一（分と秒が混ざらない）
- ボタンの幅は画面の実寸から計算する（`(画面幅 − 余白 − 間隔) / 2`）
- Digital Crown は**1目盛り＝10秒**。秒で持って by: 1 にすると目盛りの触覚が1秒ごとに鳴り、回しても動かないように感じた
- チップも「開始」も無し。やめるときはクラウンを押して文字盤へ（裏へ回った時点で設定は閉じる）
- 完了したら保存して最初から
- ＋− の触覚は `.click`（`.stop` は二重に震える）
- 「色 n」ボタン：`ThemeHex.all` の 10 組を順繰り。App Group の `theme` に保存し、コンプリケーションも同じ色になる

### おわり
- 地が白く抜ける（`#E9F3F1`）。濃いティールで `0` と「おわり」
- タップで最初から

## 合図

| いつ | Watch | iPhone |
|---|---|---|
| 開始 | `.start` | heavy |
| 終了（前に出ている） | `.notification`×3（110ms）→ 220ms → `.success` | success → heavy×2 |
| 終了（裏） | 通知1回（システムの触覚） | 通知1回 |
| ＋− | `.start` / `.stop` | soft |
| チップ | `.click` | rigid |

前に出ている間は `UNUserNotificationCenterDelegate.willPresent` が `[]` を返して通知を抑える。
裏では `[.banner, .sound]`。

## 画面の出入り（`scenePhase` の3つを区別する）

| いつ | どうする |
|---|---|
| `.active` | 見ている。**アプリの外から開いたときだけ**、ここで走り出す |
| `.inactive` | 腕を下ろして暗い。タイマーはそのまま。合図は通知に任せる（`gate` を下ろす） |
| `.background` | クラウンで出た。**走っているものを止め、通知の予約も消す** |

「アプリの外から開いた」を持っておかないと、**腕を下ろして上げただけで新しいタイマーが走り出す。**

開いたときの分岐。

| 状態 | 動き |
|---|---|
| 走っている | 続き |
| 終わっている（自然終了・キャンセルとも） | **新しく始める。** 見せ直す意味がない |

腕を上げただけで始まらないのは「アプリの外から開いたか」を持っているから（下の表）。
猶予時間のような仕掛けは要らない。

「終わった時刻」は追いついた時刻ではなく本当の終了時刻（`endAt`）で記録する。

## 保存

UserDefaults。`duration`（秒）と `engine`（JSON：duration / endAt / finishedAt）。

## コンプリケーション（2026-09-11 決定）

6案（`Tools-MakeComplication.swift` で実寸を並べて比較）から **E「マーク＋数字」**。

- 丸の中に、上がストップウォッチのマーク（色の組の色）、下が数字（白）
- 数字は**設定してある秒数**（`90`）だけ。**残り時間は出さない**
  （アプリから出た時点でタイマーは止まるので、文字盤を見ているときに走っていることはあり得ない。
  カウントダウンを出したら、クラウンで抜けた直後に古い残り時間が一瞬残って見えた）
- 描き直しは**設定が変わったときだけ**アプリ側から頼む。開始・終了では頼まない
- 縁の輪は **3pt**。1.5pt では文字盤に埋もれた（`Tools-MakeComplication.swift` で実寸を並べて決めた）
- 単色に着色される文字盤ではマークだけに色が乗る（`widgetAccentable`）
- **`GeometryReader` を使わない。** 枠が小さく 0 や NaN が返ることがある（引き継ぎ書 4-86）
