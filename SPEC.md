# ワンタップタイマー 実装仕様

決めたこと（2026-09-09）。プロトタイプ `prototype/index.html` の B 案「ドレイン」。

## 要件

- **長押しで止める**（0.7秒）。止めた画面は「キャンセル」と設定の長さ
- **裏では動かさない。** クラウンを押して出た時点でタイマーも予約した通知も片付ける
- 終了後に**アプリを自分で閉じる仕組みは watchOS に無い**（公開 API が無い）。
  クラウンを押す＝キャンセルして文字盤へ、という形で代える

- **ワンタップ**：開いたら設定済みの長さで即開始。設定画面を経ない
- 終了は**一度だけ**震える。鳴り続けない。自分で触らなくてよい
- 走っている最中に**設定画面**へ入れる（左上の歯車。上部に、目立たない質感で、押しやすい大きさ）
- 長さは **10秒〜60分**。初期値 90秒
- **止めるボタンは無い**。画面タップは最初から
- **1分を切ったら秒だけ**を出す
- App Store に出す（iOS の器つき）

## 画面

### 走っている
- 地は黒。下から溜まった水（ティール）が、残りの割合ぶんの高さ。水面に白い線
- 真ん中に残り。`m:ss`（`1:30`）。1分を切ったら秒だけ（`45`）を一回り大きく
- 数字の下に「のこり」／「秒」
- 最後の10秒は水が琥珀（長さが20秒以下なら出さない）
- 上の中央に丸ボタン（42pt。ストップウォッチの絵に ± の印。システム時刻は右上なので中央は空いている）
- 走っている最中のタップは何もしない。長押し（0.7秒）でキャンセル。下端に「長押しでキャンセル」
- 終わった／キャンセルした画面はタップで新しく始める（下端に「タップで始める」）

### 設定（2026-09-10 実機で触って2度作り直し）
- `1分30秒` ／ **同じ大きさの4つ**（−10秒・+10秒／−1分・+1分。2×2）／ 下段に **色の丸**と**完了**
- ボタンの幅は画面の実寸から計算する（`(画面幅 − 余白 − 間隔) / 2`）
- Digital Crown は**1目盛り＝10秒**。秒で持って by: 1 にすると目盛りの触覚が1秒ごとに鳴り、回しても動かないように感じた
- チップも「開始」も無し。やめるときはクラウンを押して文字盤へ（裏へ回った時点で設定は閉じる）
- 完了したら保存して最初から
- ＋− の触覚は `.click`（`.stop` は二重に震える）
- 「色 n」ボタン：`ThemeHex.all` の 10 組を順繰り。App Group の `theme` に保存し、コンプリケーションも同じ色になる

### おわり
- 地が白く抜ける（`#E9F3F1`）。濃いティールで `0` と「おわり」
- タップで最初から

## 合図

| いつ | Watch | iPhone |
|---|---|---|
| 開始 | `.start` | heavy |
| 終了（前に出ている） | `.notification`×3（110ms）→ 220ms → `.success` | success → heavy×2 |
| 終了（裏） | 通知1回（システムの触覚） | 通知1回 |
| ＋− | `.start` / `.stop` | soft |
| チップ | `.click` | rigid |

前に出ている間は `UNUserNotificationCenterDelegate.willPresent` が `[]` を返して通知を抑える。
裏では `[.banner, .sound]`。

## 画面の出入り（`scenePhase` の3つを区別する）

| いつ | どうする |
|---|---|
| `.active` | 見ている。**アプリの外から開いたときだけ**、ここで走り出す |
| `.inactive` | 腕を下ろして暗い。タイマーはそのまま。合図は通知に任せる（`gate` を下ろす） |
| `.background` | クラウンで出た。**走っているものを止め、通知の予約も消す** |

「アプリの外から開いた」を持っておかないと、**腕を下ろして上げただけで新しいタイマーが走り出す。**

開いたときの分岐。

| 状態 | 動き |
|---|---|
| 走っている | 続き |
| キャンセル済み | すぐ新しく始める（やめたものを見せ直しても仕方がない） |
| 自然に終わってから 30秒以内 | 「おわり」のまま（腕を上げて確かめただけで次が走らないように） |
| 自然に終わってから 30秒より後 | 新しく始める |

「終わった時刻」は追いついた時刻ではなく本当の終了時刻（`endAt`）で記録する。

## 保存

UserDefaults。`duration`（秒）と `engine`（JSON：duration / endAt / finishedAt）。
